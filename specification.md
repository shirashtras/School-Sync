
## 1. מטרות המערכת

המערכת נועדה לספק פתרון מלא, יציב וגמיש לניהול מערכת שעות בית־ספרית, עבור כמה סוגי מערכות PDF שונים, בלי שכל שינוי קטן בעיצוב ישבור את הפריסה או את הנתונים.

**יכולות עיקריות:**

- **טעינה חד־פעמית** של קובץ מערכת (PDF / Excel).
- **המרה ל־JSON אחיד ותקני** ללא תלות בעיצוב ה־PDF.
- **תמיכה במגוון פורמטים**:
  - מערכות עם 32 כיתות.
  - מערכות שחור־לבן.
  - מערכות קטנות (3 כיתות / כמה שכבות).
  - מערכות עם ימים בצד, בשורה, משולבים עם שעות.
- **תמיכה בכיתות מכל סוג:**
  - עם מספרים: `א1`, `א2`, `ה1`, `ה2`, `ז1`…
  - בלי מספרים: `ב`, `ג`, `ד`, `ז`, `ח`.
  - כיתות מיוחדות: `I`, `27`, `47` וכו'.
- **תמיכה מלאה במקצועות, מורות והקבצות**:
  - כמה מורות באותו תא.
  - כמה כיתות באותו תא.
  - שעות כפולות / משולשות.
- **תצוגה לפי:**
  - כיתה
  - מורה
- **עריכת שיעור** בממשק נוח.
- **הדפסה גמישה** (בחירת כיתות / מורות / פריסה).
- **אפליקציית Desktop** (Electron) – ללא תלות בדפדפן.

---

## 2. תהליך עבודה (Flow)

1. המשתמשת פותחת את האפליקציה (Electron).
2. האפליקציה בודקת האם קיימת מערכת טעונה (JSON שמור בדיסק מקומי).
   - אם **אין מערכת** → מוצג מסך טעינת קובץ.
   - אם **יש מערכת** → מוצג תפריט ראשי.
3. המשתמשת מעלה קובץ מערכת (PDF / Excel).
4. צד השרת (Node.js):
   - קורא את הקובץ.
   - ממיר את ה־PDF לטבלה גולמית (rows/cols, כולל טקסט, מיזוגי תאים).
   - מפעיל **Parser אוניברסלי** שמייצר JSON אחיד.
   - שומר את ה־JSON בקובץ פנימי (למשל `schedule.json`).
5. צד הלקוח (React):
   - טוען את ה־JSON ל־`ScheduleContext`.
   - מציג תפריט ראשי.
6. המשתמשת בוחרת תצוגה:
   - מערכת לפי כיתה / מורה .
7. ניתן:
   - לערוך שיעור (subject/teacher/group).
   - לשמור שינויים (עדכון JSON).
   - להדפיס מערכות.

---

## 3. מבנה נתונים פנימי (JSON)

### 3.1 רשומת שיעור בסיסית

```json
{
  "className": "א1",
  "day": "ראשון",
  "hour": 3,
  "subject": "חשבון",
  "teacher": "קלרברג",
  "group": null,
  "rawCellText": "חשבון קלרברג, שפירא לאה",
  "source": {
    "fileName": "פריסה 32 כיתות.pdf",
    "page": 2,
    "rowIndex": 17,
    "colIndex": 5
  }
}
3.2 כללים:
לכל כיתה → רשומה נפרדת.

לכל מורה → רשומה נפרדת.

שדה group:

null – אין הקבצה.
 – לפי סדר הופעת המורות/הכיתות בתא.

שדה rawCellText – תמיד שומר את הטקסט המקורי מהתא לצורך Debug.

שדה source – מאפשר איתור מקור השיעור ב־PDF במקרה של באגים.

4. Parser – עקרונות כלליים
ה־Parser חייב להיות אוניברסלי ולא תלוי בעיצוב ספציפי.
הוא עובד בשלבים:

קריאת טבלה גולמית:

מערך של שורות.

כל שורה = מערך תאים.

כל תא כולל:

טקסט (יכול להיות כמה שורות).

מידע על מיזוג (rowspan, colspan).

זיהוי שורת כיתות (Headers).

זיהוי בלוקי ימים.

זיהוי שעות בתוך כל יום.

פענוח כל תא שיעור:

מקצוע.

מורה/מורות.

כיתה/כיתות.

הקבצות.

יצירת רשומות JSON.

5. זיהוי כיתות (Class Headers)
5.1 כללים
שורת הכותרת העליונה שבה רוב התאים אינם ריקים נחשבת שורת כיתות.

כל תא לא ריק בשורה זו → כיתה.

כיתה יכולה להיות:

א1, א2, א3, א4, א5, א6…

ב, ג, ד, ה, ו, ז, ח (ללא מספר).

I, 27, 47, ב6 וכו'.

אין דרישה לאחידות בין הכיתות.

5.2 אלגוריתם
לעבור על השורות מלמעלה.

לכל שורה:

לספור כמה תאים אינם ריקים.

השורה הראשונה שבה מספר התאים הלא־ריקים ≥ סף (למשל 4) → שורת כיתות.

לשמור:

classes[] = [ "א1", "א2", "ב", "ג", "ד", "ה1", "ה2", "I", "ז", "ח", ... ]

אינדקס עמודה לכל כיתה.

6. זיהוי ימים (Day Blocks)
המערכת תומכת בכמה צורות הופעת ימים:

6.1 ימים בשורה (כותרת יום)
דוגמה:
ראשון בשורה נפרדת, מעל השעות.

6.2 ימים בצד (rowspan)
דוגמה:
1 2 3 4 ראשון 5 6 7 בתא צדדי עם rowspan גדול.

6.3 ימים משולבים בשעה
דוגמה:
4 ראשון – תא שמכיל גם מספר שעה וגם שם יום.

6.4 רשימת ימים חוקיים
ראשון

שני

שלישי

רביעי

חמישי

שישי

6.5 אלגוריתם זיהוי יום (אוניברסלי)
לעבור על כל השורות.

בכל שורה, לבדוק את כל התאים:

אם הטקסט מכיל אחד משמות הימים → התחלת בלוק יום חדש.

לכל בלוק יום:

לשמור:

dayName (למשל "ראשון").

startRowIndex.

endRowIndex:

אם יש rowspan → לפי גודל ה־rowspan.

אם אין → עד שמופיע יום חדש או סוף הטבלה.

בתוך כל בלוק יום:

השורה הראשונה אחרי שורת היום = שעה 1.

השורה השנייה = שעה 2.

וכן הלאה.

7. זיהוי שעות (Hour Indexing)
המערכת לא מסתמכת על מספרי שעות בקובץ, אלא על המיקום היחסי בתוך בלוק היום.

7.1 כללים
בתוך כל בלוק יום:

שורה 1 (אחרי שורת היום) → hour = 1

שורה 2 → hour = 2

שורה 3 → hour = 3

...

אם בתא הצדדי כתוב: 5 6 או 5 6 7:

זה מצביע על שיעור כפול/משולש.

ב־JSON:

hour = השעה הראשונה (5).

ניתן להוסיף שדה אופציונלי:

duration: 2 (לשיעור כפול)

duration: 3 (לשיעור משולש)

אם לא נדרש – אפשר להשאיר בלי duration ולהסתמך על השעה בלבד.

8. פענוח תא – מקצוע, מורה, כיתות, הקבצות
זה החלק הקריטי שבו נוצרות השיבושים אם לא מגדירים טוב.

8.1 מבנה תא אפשרי
סוג	דוגמה	פענוח
מקצוע בלבד	יהדות	subject = "יהדות"
מקצוע + מורה	יהדות ויטמן	subject = "יהדות", teachers = ["ויטמן"]
מקצוע + כמה מורות	חשבון קלרברג, שפירא לאה	subject = "חשבון", teachers = ["קלרברג", "שפירא לאה"]
מקצוע + מורה בשורה נפרדת	שורה 1: תורה שורה 2: הורביץ	subject = "תורה", teachers = ["הורביץ"]
כמה כיתות בתא	א2, א6 שעת סיפור	classNames = ["א2", "א6"], subject = "שעת סיפור"
כמה מורות + כמה כיתות	א1, א3 שעת סיפור 3 דויטש	classNames = ["א1", "א3"], subject = "שעת סיפור 3", teachers=["דויטש"]
מקצוע + תיאור נוסף	באורי תפילה פרקי אבות	subject = "באורי תפילה פרקי אבות"
מקצוע + הקבצות מרובות	אנגלית אהרוני, לויסון, פקרביץ	subject = "אנגלית", teachers = ["אהרוני","לויסון","פקרביץ"]


8.2 אלגוריתם פענוח תא – שלבים
8.2.1 איחוד שורות
כל תא יכול להכיל כמה שורות (ב־PDF).

מאחדים את כל השורות לטקסט אחד:

מחברים עם רווח: "תורה" + "הורביץ" → "תורה הורביץ".

שומרים גם את הרשימה המקורית אם צריך.

8.2.2 ניקוי טקסט
הסרת רווחים כפולים.

הסרת רווחים לפני/אחרי פסיקים.

השארת פסיקים כ־Separator ל:

כיתות.

מורות.

8.2.3 זיהוי כיתות מתוך הטקסט (אם קיימות)
לבדוק האם הטקסט מכיל שמות כיתות (מתוך classes[]).

אם כן:

להוציא אותן לרשימת classNamesFromText.

להסיר אותן מהטקסט לצורך המשך פענוח.

אם לא:

className = הכיתה של העמודה (מה־Header).

8.2.4 זיהוי מקצוע
רשימת מקצועות ידועה (ניתן להרחבה בקובץ קונפיגורציה):

תורה, נביא, יהדות, דינים,
חשבון, הנדסה, אנגלית,
טבע, גאוגרפיה, היסטוריה,
ספרות, דקדוק, הבעה,
מולדת, חברה, כישורי חיים,
שעת סיפור, זמרה, ריתמיקה,
מלאכה, ציור, ספורט,
באורי תפילה, פרקי אבות,
ועוד.

חיפוש המקצוע בטקסט:

אם יש כמה מילים שמהוות מקצוע (למשל באורי תפילה פרקי אבות) – לזהות כיחידה אחת.

אם לא נמצא מקצוע ברור – לשמור את כל הטקסט כ־subject ולהשאיר teacher = null (Fail-safe).

8.2.5 זיהוי מורות
אחרי הסרת הכיתות והמקצוע מהטקסט:

מה שנשאר → שמות מורות, מופרדים בפסיקים או ברווחים.

פיצול לפי פסיקים:

לדוגמה: "קלרברג, שפירא לאה" → ["קלרברג", "שפירא לאה"].

ניקוי רווחים.

אם אין מורות בטקסט:

teacher = null (אפשרי במערכות מסוימות).

8.2.6 יצירת רשומות JSON
קביעת רשימת כיתות:

אם classNamesFromText לא ריק → להשתמש בו.

אחרת → className = כיתה מהעמודה.

לכל כיתה ולכל מורה:

ליצור רשומה נפרדת.

הקבצות:

אם יש כמה מורות באותו תא:

לפי סדר הופעה:

מורה ראשונה → group = "קבוצה 1"

מורה שנייה → group = "קבוצה 2"

מורה שלישית → group = "קבוצה 3"

אם יש כמה כיתות באותו תא:

כל כיתה מקבלת רשומה נפרדת, עם אותו group עבור אותה מורה.

9. תמיכה בשעות כפולות / משולשות
9.1 עיקרון
אם במערכת המקורית השיעור מופיע כשיעור כפול (למשל 5 6), אסור להשאיר ריק בשעה 6 – צריך לשמר את ההתנהגות.

9.2 אלגוריתם
אם בתא הצדדי (שעות) מופיע טקסט כמו:

5 6

5 6 7

אז:

baseHour = 5

duration = מספר השעות (2 או 3).

ב־JSON:

אפשרות א':

רשומה אחת עם hour = 5, duration = 2.

אפשרות ב' (אם רוצים מפורט):

שתי רשומות:

hour = 5

hour = 6

שתיהן עם אותו subject, teacher, className, group.

החלטה במערכת זו:

נשמר רשומה אחת עם hour = השעה הראשונה.

נוסיף שדה אופציונלי:

duration: 2 / duration: 3.

בצד התצוגה, אם רוצים למלא גם את השעה הבאה – עושים זאת לוגית.

10. מסכי המערכת
10.1 מסך טעינת קובץ
בחירת קובץ PDF / Excel.

הצגת סטטוס:

"קובץ נטען"

"Parsing…"

"המערכת מוכנה".

10.2 תפריט ראשי
כפתורים:

"מערכת לפי כיתה"

"מערכת לפי מורה"

"מערכת לפי יום"

"טעינת קובץ חדש"

"הדפסה"

10.3 מערכת לפי כיתה
בחירת כיתה מרשימה.

הצגת טבלה:

עמודות: ימים.

שורות: שעות.

בכל תא: מקצוע + מורה (או כמה מורות).

כפתור "עריכה" בכל תא.

10.4 מערכת לפי מורה
בחירת מורה מרשימה.

הצגת טבלה:

עמודות: ימים.

שורות: שעות.

בכל תא: מקצוע + כיתה/כיתות + group.

10.5 מערכת לפי יום
בחירת יום.

הצגת טבלה:

שורות: שעות.

עמודות: כיתות / מורות (לפי מצב תצוגה).

10.6 עריכת שיעור
בחירת תא.

טופס:

כיתה (לא חובה לשנות).

יום.

שעה.

מקצוע (Dropdown + אפשרות הקלדה חופשית).

מורה (Dropdown + אפשרות הקלדה).

group (בחירה / טקסט חופשי).

שמירה → עדכון JSON.

10.7 הדפסה
בחירת:

כיתות להדפסה.

מורות להדפסה.

פריסה:

1 בעמוד.

2 בעמוד.

4 בעמוד.

6 בעמוד.

יצירת PDF להדפסה.

11. API צד שרת
http
GET  /api/schedule/status
POST /api/schedule/upload
GET  /api/schedule/classes
GET  /api/schedule/teachers
GET  /api/schedule/days
GET  /api/schedule/day/:day
GET  /api/schedule/class/:className
GET  /api/schedule/teacher/:teacherName
PUT  /api/schedule/update
upload:

מקבל קובץ.

מריץ Parser.

שומר JSON.

update:

מקבל רשומת שיעור מעודכנת.

מעדכן ב־JSON.

12. עקרונות יציבות נגד "שיבושים"
כדי למנוע מצב שבו "המקצועות והמורות והכיתות הכל משובש", ה־Parser חייב לעמוד בכללים הבאים:

לא להסתמך על מיקום ויזואלי בלבד:

תמיד לעבוד עם:

שורת כיתות.

זיהוי יום לפי טקסט.

זיהוי שעה לפי אינדקס בתוך היום.

לא להניח שכל תא = מקצוע + מורה אחת:

תמיד לתמוך:

בכמה מורות.

בכמה כיתות.

בשורות מרובות.

שימור הטקסט המקורי:

rawCellText נשמר תמיד.

מאפשר Debug אם משהו לא מובן.

Fail-safe:

אם לא מצליחים לזהות מורה:

לשמור subject כמו שהוא.

teacher = null.

אם לא מצליחים לזהות כיתה מתוך הטקסט:

להשתמש בכיתה מהעמודה.

תמיכה בכיתות ללא מספרים:

אין תלות בתבנית אות+מספר.

כל טקסט בשורת הכיתות נחשב כיתה.

תמיכה בפסיקים:

פסיקים משמשים להפרדת:

כיתות.

מורות.

האלגוריתם תמיד:

קודם מחפש כיתות.

אחר כך מקצוע.

אחר כך מורות.