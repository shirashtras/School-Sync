# 📘 מסמך אפיון סופי – מערכת תזמון בית ספרית

מערכת Desktop לניהול, הצגה ועריכה של מערכת שעות בית־ספרית.  
המערכת מבוססת Electron + React + Node.js.

---

## 1. מטרות המערכת

המערכת מאפשרת:

- טעינה חד־פעמית של קובץ מערכת (Excel / PDF).
- המרת הקובץ ל־JSON ושמירתו בשרת.
- טעינת הנתונים ל־ScheduleContext בצד הלקוח.
- הצגת מערכת לפי:
  - כיתה
  - מורה
  - יום
- עריכת שיעור בממשק פשוט.
- הדפסה של מערכות.
- עבודה כאפליקציית Desktop.

---

## 2. תהליך עבודה (Flow)

1. המשתמשת פותחת את התוכנה.
2. המערכת בודקת האם קיימת מערכת טעונה:
   - אם **אין מערכת טעונה** → מוצג מסך טעינת קובץ.
   - אם **קיימת מערכת טעונה** → מוצג תפריט ראשי.
3. המשתמשת טוענת קובץ מערכת.
4. השרת:
   - קורא את הקובץ
   - ממיר ל־JSON
   - שומר בקובץ פנימי
5. הלקוח:
   - טוען את הנתונים ל־ScheduleContext
6. מוצג תפריט ראשי.
7. המשתמשת בוחרת תצוגה.
8. המערכת מציגה טבלה מסודרת.
9. המשתמשת יכולה לערוך שיעור.
10. המערכת שומרת את השינויים ומעדכנת Context.
11. ניתן להדפיס.

---

## 3. מבנה נתונים פנימי (JSON)

```json
{
  "className": "א1",
  "day": "ראשון",
  "hour": 2,
  "subject": "חשבון",
  "teacher": "שפירא לאה",
  "group": "קבוצה א"
}
